apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: node-red-cert
  namespace: node-red
spec:
  secretName: node-red-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - nodered.feto.dev
